// Re-exports the shared vocabulary for convenience inside the web
// app — so a component can `import { DATA_TYPES } from "../types"`
// instead of reaching into the shared package path directly.
export * from "@dataflow-guardian/shared";